# Car Rental Backend

Basic Express server setup for the car rental frontend.

## Setup

```bash
npm install
npm run dev
```

The server starts on `http://localhost:4000` by default.

## Routes

- `GET /health`
